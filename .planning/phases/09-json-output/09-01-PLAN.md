---
phase: 09-json-output
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/output/json-formatter.ts
  - src/cli.ts
  - tests/unit/output/json-formatter.test.ts
  - tests/integration/cli-pipeline.test.ts
autonomous: true
requirements: [OUT-01, OUT-03, OUT-04]

must_haves:
  truths:
    - "Running --output json --dry-run produces valid JSON on stdout with version, success, dryRun, and tests array"
    - "Running --output json with a real run produces valid JSON on stdout with version, success, exitCode, metadata, summary, and tests array"
    - "Human-readable progress on stderr continues simultaneously when --output json is active"
    - "Commander --help and --version output goes to stderr, stdout is empty"
    - "Unknown --output format exits 2 with descriptive error on stderr"
    - "Runtime errors with --output json still produce valid parseable JSON on stdout"
  artifacts:
    - path: "src/output/json-formatter.ts"
      provides: "formatJsonOutput, formatJsonDryRun, formatJsonError functions and JsonOutput/JsonOutputMetadata types"
      exports: ["formatJsonOutput", "formatJsonDryRun", "formatJsonError", "JsonOutput", "JsonOutputMetadata"]
    - path: "tests/unit/output/json-formatter.test.ts"
      provides: "Unit tests for all three formatter functions"
      min_lines: 80
    - path: "tests/integration/cli-pipeline.test.ts"
      provides: "Integration tests for --output json, --help stderr redirect, unknown format"
      contains: "output json"
  key_links:
    - from: "src/cli.ts"
      to: "src/output/json-formatter.ts"
      via: "import formatJsonOutput, formatJsonDryRun, formatJsonError"
      pattern: "import.*json-formatter"
    - from: "src/cli.ts"
      to: "process.stdout"
      via: "process.stdout.write(json) after runner.run() and in dry-run path"
      pattern: "process\\.stdout\\.write"
    - from: "src/cli.ts"
      to: "writeStderr"
      via: "program.configureOutput({ writeOut: (str) => writeStderr(str.trimEnd()) })"
      pattern: "configureOutput"
---

<objective>
Add `--output json` flag that produces a single valid JSON object on stdout containing version, success, exitCode, metadata, summary stats, and full test results. Human-readable progress continues on stderr simultaneously. Commander help/version is redirected to stderr unconditionally.

Purpose: Enable programmatic consumption of test results by CI tools, scripts, and `jq` pipelines (OUT-01, OUT-03, OUT-04).
Output: `src/output/json-formatter.ts` with pure formatter functions, updated `src/cli.ts` with --output flag and configureOutput, unit and integration tests.
</objective>

<execution_context>
@/Users/luismorales/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luismorales/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-json-output/09-CONTEXT.md
@.planning/phases/09-json-output/09-RESEARCH.md

@src/cli.ts
@src/output/reporter.ts
@src/output/types.ts
@src/runner/types.ts
@tests/integration/cli-pipeline.test.ts
@tests/unit/output/reporter.test.ts

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/runner/types.ts:
```typescript
export type TestStatus = "passed" | "failed";
export type TestSource = "cache" | "ai";

export interface TestResult {
  testName: string;
  testCase: string;
  status: TestStatus;
  source: TestSource;
  durationMs: number;
  error?: string;
  selfHealed?: boolean;
}

export interface RunResult {
  results: TestResult[];
  totalDurationMs: number;
  passed: number;
  failed: number;
  cached: number;
  skipped: number;
}
```

From src/output/reporter.ts:
```typescript
export function writeStderr(text: string): void;
```

From src/cli.ts (line 43):
```typescript
const program = new Command();
```

Commander configureOutput API (from commander v14 typings):
```typescript
program.configureOutput({
  writeOut: (str: string) => void,
  writeErr: (str: string) => void,
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create json-formatter.ts with types and unit tests</name>
  <files>src/output/json-formatter.ts, tests/unit/output/json-formatter.test.ts</files>
  <behavior>
    - formatJsonOutput: Given a RunResult with 2 passed + 1 failed test, metadata, version "0.2.0", exitCode 1 -> returns valid JSON string with success: false, exitCode: 1, correct summary counts, 3 test entries with correct fields
    - formatJsonOutput: selfHealed field only appears when true, error field only appears when present
    - formatJsonOutput: filter metadata included when provided, omitted when not
    - formatJsonDryRun: Given test list with 2 tests (1 cache, 1 ai), metadata, version -> returns valid JSON with dryRun: true, success: true, exitCode: 0, summary.total and summary.cached counts, test entries with testName/testCase/source
    - formatJsonError: Given error message "baseUrl unreachable", version, partial metadata -> returns valid JSON with success: false, exitCode: 2, error field set, empty tests array, zero summary counts
    - All functions return strings that JSON.parse() can parse without throwing
    - camelCase field names throughout (per TypeScript convention)
    - Timestamps in ISO 8601 format
    - Pretty-printed with 2-space indent
  </behavior>
  <action>
    **Write tests first** in `tests/unit/output/json-formatter.test.ts`. Use `bun:test` with `describe/test/expect`. Import the three formatter functions and the type interfaces. Create mock `RunResult` objects using the existing types from `src/runner/types.ts`. Test each function with the behaviors listed above. Run tests -- they must FAIL (red).

    **Then implement** `src/output/json-formatter.ts`:
    1. Export `JsonOutputMetadata` interface: model, provider, configFile, baseUrl (string | undefined), timestamp (string, ISO 8601), optional filter ({ pattern, matched, total })
    2. Export `JsonOutput` interface: version, success, exitCode, dryRun?, metadata, summary ({ passed, failed, cached, skipped, totalDurationMs }), tests (array with testName, testCase, status, source, durationMs, selfHealed?, error?), error? (top-level for runtime errors)
    3. Export `formatJsonOutput(runResult: RunResult, metadata: JsonOutputMetadata, version: string, exitCode: number): string` -- maps RunResult fields to JsonOutput, only includes selfHealed when true, only includes error when present. Uses `JSON.stringify(output, null, 2)`.
    4. Export `formatJsonDryRun(tests: Array<{ name: string; case: string; source: "cache" | "ai" }>, metadata: JsonOutputMetadata, version: string): string` -- produces JsonOutput with dryRun: true, exitCode: 0, success: true, summary with total and cached counts.
    5. Export `formatJsonError(errorMessage: string, version: string, metadata: Partial<JsonOutputMetadata>): string` -- produces JsonOutput with success: false, exitCode: 2, error field, empty tests, zero summary.

    Run tests again -- they must PASS (green). Run `bunx biome check src/output/json-formatter.ts tests/unit/output/json-formatter.test.ts` to lint.
  </action>
  <verify>
    <automated>cd /Users/luismorales/project/super/super-ghost && bun test tests/unit/output/json-formatter.test.ts && bunx biome check src/output/json-formatter.ts tests/unit/output/json-formatter.test.ts</automated>
  </verify>
  <done>All unit tests pass for formatJsonOutput, formatJsonDryRun, and formatJsonError. Functions return valid JSON with correct shape. Biome clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wire --output flag into CLI, configureOutput, and integration tests</name>
  <files>src/cli.ts, tests/integration/cli-pipeline.test.ts</files>
  <action>
    **Part A -- Commander configureOutput (OUT-04):**
    In `src/cli.ts`, immediately after `const program = new Command();` (line 43) and before the `.name()` chain, add:
    ```
    program.configureOutput({
      writeOut: (str) => writeStderr(str.trimEnd()),
      writeErr: (str) => writeStderr(str.trimEnd()),
    });
    ```
    This redirects help/version output to stderr unconditionally (matches Phase 7 "stdout reserved" decision). Applied before any parsing so it takes effect for --help/--version.

    **Part B -- Add --output flag:**
    Add `.option("--output <format>", "Output format (json)")` to the Commander chain (after line 54, before `.exitOverride`).
    Add `output?: string` to the options type in the action signature (line 63-70).

    **Part C -- Format validation (early in action):**
    At the top of the action handler, after the ProcessManager setup (line 71-72), add format validation:
    ```
    if (options.output && options.output !== "json") {
      writeStderr(`${pc.red("Error:")} Unknown output format '${options.output}'. Supported: json`);
      setTimeout(() => process.exit(2), 100);
      return;
    }
    ```

    **Part D -- JSON dry-run output (OUT-01):**
    In the dry-run section (around line 110-139), after the existing dry-run logic, add a JSON path. When `options.output === "json"`, instead of writing human-readable dry-run to stderr, build the test list with name/case/source and call `formatJsonDryRun()`, then `process.stdout.write(json + "\n")` and exit. The human-readable stderr output from the reporter should still happen (OUT-03), so do NOT skip the stderr output -- only ADD the stdout JSON write. Actually, re-reading the dry-run code: the dry-run section writes its own output to stderr via `writeStderr`. When `--output json` is active, still let those stderr writes happen (they're human progress), and ALSO write JSON to stdout before exiting. Import `formatJsonDryRun` from `./output/json-formatter.ts`.

    **Part E -- JSON run output (OUT-01):**
    After `runner.run()` completes and before `process.exit(code)` (around line 201-207), when `options.output === "json"`:
    1. Build `JsonOutputMetadata` from config/options: model, provider, configFile: options.config, baseUrl: config.baseUrl, timestamp: new Date().toISOString(), and filter info if options.only is set
    2. Call `formatJsonOutput(result, metadata, pkg.version, code)`
    3. `process.stdout.write(json + "\n")`
    Import `formatJsonOutput` and `JsonOutputMetadata` from `./output/json-formatter.ts`.

    **Part F -- JSON error output:**
    In the catch block (line 208-227), when `options.output === "json"`, emit valid JSON to stdout before exiting. Note: `options` may not be in scope in the catch block since it's defined in the action parameter. The catch IS inside the action, so `options` is accessible. For each error path (ConfigLoadError, Missing API key, unexpected error), add:
    ```
    if (options.output === "json") {
      const json = formatJsonError(msg, pkg.version, { configFile: options.config });
      process.stdout.write(json + "\n");
    }
    ```
    Import `formatJsonError` from `./output/json-formatter.ts`.

    **Part G -- Update existing integration tests (OUT-04 breaking change):**
    In `tests/integration/cli-pipeline.test.ts`:
    - Lines 58-63: `--help` test currently checks `stdout` -- change to check `stderr` instead, and add `expect(stdout).toBe("")`
    - Lines 65-69: `--version` test currently checks `stdout` -- change to check `stderr` instead, and add `expect(stdout).toBe("")`
    - Lines 92-97: `--help shows --only and --no-cache` test checks `stdout` -- change to `stderr`
    - Lines 154-158: `--help shows --dry-run option` test checks `stdout` -- change to `stderr`
    - Lines 293-298: `--help shows --verbose option` test checks `stdout` -- change to `stderr`

    **Part H -- Add new integration tests:**
    Add a new `describe("output json", ...)` block in `cli-pipeline.test.ts`:

    1. `--output json --dry-run produces valid JSON on stdout` -- run with multi-test-config.yaml, --output json, --dry-run. Parse stdout with JSON.parse(). Assert: version === "0.2.0", success === true, dryRun === true, tests is array with 4 items, each has testName/testCase/source. Assert stderr is non-empty (OUT-03).

    2. `--output json --dry-run --only filters correctly` -- run with multi-test-config.yaml, --output json, --dry-run, --only "Login*". Parse stdout JSON. Assert metadata.filter exists with pattern/matched/total, tests has 2 items.

    3. `--help outputs to stderr not stdout` -- run with --help. Assert stdout === "" and stderr contains "--config" (verifies OUT-04).

    4. `--version outputs to stderr not stdout` -- run with --version. Assert stdout === "" and stderr contains "0.2.0".

    5. `unknown --output format exits 2` -- run with --config valid-config.yaml, --output csv. Assert exitCode 2, stderr contains "Unknown output format", stderr contains "csv".

    6. `--output json with missing API key emits error JSON` -- run with --config valid-config.yaml, --output json, OPENAI_API_KEY="". Assert exitCode 2, parse stdout JSON, assert success === false, exitCode === 2, error contains "Missing API key".

    7. `--output json --dry-run stderr still shows progress` -- run with multi-test-config.yaml, --output json, --dry-run. Assert stderr contains test names (human-readable output continues).

    Run `bunx biome check src/cli.ts tests/integration/cli-pipeline.test.ts` to lint. Run full test suite `bun test` to verify nothing is broken.
  </action>
  <verify>
    <automated>cd /Users/luismorales/project/super/super-ghost && bun test && bunx biome check src/cli.ts src/output/json-formatter.ts tests/unit/output/json-formatter.test.ts tests/integration/cli-pipeline.test.ts</automated>
  </verify>
  <done>
    - `--output json --dry-run` produces valid JSON on stdout (parseable by JSON.parse) with version, success, dryRun, tests array
    - `--help` and `--version` output goes to stderr, stdout is empty
    - Unknown format `--output csv` exits 2 with descriptive error
    - `--output json` with error conditions emits valid JSON with success: false
    - Human-readable stderr progress continues when --output json is active
    - All existing tests pass after help/version assertions updated to check stderr
    - Biome clean on all modified files
  </done>
</task>

</tasks>

<verification>
1. `bun test` -- full test suite passes (unit + integration)
2. `bun run lint` -- Biome clean on all files
3. Manual spot check: `bun run src/cli.ts --help 2>/dev/null` produces empty output (stdout reserved)
4. Manual spot check: `bun run src/cli.ts --config tests/fixtures/multi-test-config.yaml --output json --dry-run 2>/dev/null | jq .version` outputs "0.2.0"
</verification>

<success_criteria>
- OUT-01: `--output json` produces valid JSON on stdout with version, success, exitCode, metadata, summary, and tests array
- OUT-03: stderr shows human-readable progress simultaneously with JSON on stdout
- OUT-04: Commander help/version output goes to stderr, never stdout
- All existing tests pass (no regressions from configureOutput change)
- New unit tests cover all three formatter functions
- New integration tests cover JSON dry-run, help/version stderr, unknown format, error JSON
</success_criteria>

<output>
After completion, create `.planning/phases/09-json-output/09-01-SUMMARY.md`
</output>
