---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/output/reporter.ts
  - src/runner/test-runner.ts
  - src/infra/process-manager.ts
  - src/infra/signals.ts
  - tests/unit/output/reporter.test.ts
  - tests/unit/runner/test-runner.test.ts
  - tests/unit/infra/process-manager.test.ts
autonomous: true
requirements:
  - CLI-03
  - CLI-04
  - INFR-01
must_haves:
  truths:
    - "Each test shows spinner while running, then green checkmark with PASS or red X with FAIL, source label, and timing"
    - "Box summary displays total, passed (green), failed (red), cached, and wall time in bordered layout"
    - "Failed tests are listed below summary with error messages"
    - "Colors auto-disable when stdout is not a TTY"
    - "TestRunner executes tests sequentially and aggregates results correctly"
    - "ProcessManager tracks spawned subprocesses and kills all on shutdown"
    - "Signal handlers call ProcessManager.killAll() before exiting"
  artifacts:
    - path: "src/output/reporter.ts"
      provides: "ConsoleReporter with colors, spinners, box summary"
      exports: ["ConsoleReporter"]
      min_lines: 50
    - path: "src/runner/test-runner.ts"
      provides: "Sequential test execution orchestrator"
      exports: ["TestRunner"]
      min_lines: 30
    - path: "src/infra/process-manager.ts"
      provides: "Subprocess tracking and cleanup"
      exports: ["ProcessManager"]
      min_lines: 25
    - path: "src/infra/signals.ts"
      provides: "SIGINT/SIGTERM handler registration"
      exports: ["setupSignalHandlers"]
  key_links:
    - from: "src/output/reporter.ts"
      to: "src/output/types.ts"
      via: "implements Reporter interface"
      pattern: "implements Reporter"
    - from: "src/runner/test-runner.ts"
      to: "src/output/types.ts"
      via: "accepts Reporter in constructor"
      pattern: "reporter.*Reporter"
    - from: "src/runner/test-runner.ts"
      to: "src/config/types.ts"
      via: "accepts Config in constructor"
      pattern: "config.*Config"
    - from: "src/infra/signals.ts"
      to: "src/infra/process-manager.ts"
      via: "registers ProcessManager for cleanup on signals"
      pattern: "pm\\.killAll"
---

<objective>
Build the output reporter, test runner, and infrastructure layer -- the three components that connect config to CLI.

Purpose: ConsoleReporter provides the polished colored terminal output (spinners, box summary, failed test listing). TestRunner orchestrates sequential test execution. ProcessManager ensures no orphaned subprocesses survive after exit or Ctrl-C.
Output: All three components tested and ready for CLI wiring in Plan 03.
</objective>

<execution_context>
@/Users/luismorales/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luismorales/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Types created by Plan 01 that this plan depends on -->

From src/runner/types.ts:
```typescript
export type TestStatus = "passed" | "failed";
export type TestSource = "cache" | "ai";
export interface TestResult {
  testName: string;
  testCase: string;
  status: TestStatus;
  source: TestSource;
  durationMs: number;
  error?: string;
}
export interface RunResult {
  results: TestResult[];
  totalDurationMs: number;
  passed: number;
  failed: number;
  cached: number;
}
```

From src/output/types.ts:
```typescript
export interface Reporter {
  onTestStart(testName: string): void;
  onTestComplete(testName: string, status: TestStatus, source: TestSource, durationMs: number): void;
  onRunComplete(data: RunResult): void;
}
export type ReportData = RunResult;
```

From src/config/types.ts:
```typescript
export type TestCase = { name: string; case: string; baseUrl?: string; timeout?: number; };
export type Config = {
  baseUrl?: string; browser: "chromium" | "firefox" | "webkit"; headless: boolean;
  timeout: number; maxAttempts: number; model: string; modelProvider: string;
  cacheDir: string; recursionLimit: number; tests: TestCase[];
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ConsoleReporter with colors, spinners, and box summary</name>
  <files>src/output/reporter.ts, tests/unit/output/reporter.test.ts</files>
  <behavior>
    - onTestStart creates a spinner with the test name
    - onTestComplete stops spinner with success (green check) for passed, error (red X) for failed
    - onTestComplete output includes test name, source label, and formatted duration
    - onRunComplete prints bordered box: horizontal bars, "SuperGhost Results" title, Total/Passed/Failed/Cached/Time
    - Passed count colored green, Failed count colored red (if > 0), Time colored dim
    - Failed tests listed below summary with test name and error message
    - formatDuration: < 1000ms shows as Xms, >= 1000ms shows as X.Xs
  </behavior>
  <action>
    1. Write tests first (RED) in `tests/unit/output/reporter.test.ts`:
       - Test reporter implements Reporter interface (type check)
       - Test `onRunComplete` box output contains "SuperGhost Results", total count, passed/failed/cached counts, wall time
       - Test failed tests section appears when there are failures with test names and error messages
       - Test `formatDuration` helper: 500 -> "500ms", 1500 -> "1.5s", 10400 -> "10.4s"
       - For testing, capture console.log output by mocking it (Bun test supports jest-compatible spyOn)
       - Test that onTestStart and onTestComplete can be called without throwing (spinner behavior is visual, verified by integration)

    2. Implement `src/output/reporter.ts` (GREEN):
       - Import `pc` from "picocolors", `createSpinner` from "nanospinner"
       - Import Reporter type from "./types.ts" and TestStatus, TestSource, RunResult from "../runner/types.ts"
       - Export `class ConsoleReporter implements Reporter`
       - `onTestStart(testName)`: Create spinner with testName text and call `.start()`
       - `onTestComplete(testName, status, source, durationMs)`: Format duration string. If passed: `spinner.success({ text: \`${testName} ${pc.dim(\`(\${source}, \${formattedDuration})\`)}\` })`. If failed: `spinner.error({ text: ... })`. Set spinner to null.
       - `onRunComplete(data: RunResult)`: Print box summary using heavy horizontal lines (U+2501 repeated 40 times). Title "SuperGhost Results". Lines for Total (plain), Passed (pc.green), Failed (pc.red if > 0, plain otherwise), Cached (plain), Time (pc.dim). After box, if failed > 0, print "Failed tests:" header in red, then each failed result with "- testName" and indented dim error message.
       - Export `formatDuration(ms: number): string` helper (also used in tests)
       - IMPORTANT: Always stop spinner before writing other output (per research pitfall 3)
  </action>
  <verify>
    <automated>bun test tests/unit/output/reporter.test.ts</automated>
  </verify>
  <done>ConsoleReporter passes all unit tests: box summary contains correct sections and values, failed tests listed with errors, duration formatting correct. Reporter implements the Reporter interface from output/types.ts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TestRunner, ProcessManager, and signal handlers</name>
  <files>src/runner/test-runner.ts, src/infra/process-manager.ts, src/infra/signals.ts, tests/unit/runner/test-runner.test.ts, tests/unit/infra/process-manager.test.ts</files>
  <behavior>
    - TestRunner executes tests sequentially, calling reporter.onTestStart before each and reporter.onTestComplete after
    - TestRunner resolves per-test baseUrl: test.baseUrl ?? config.baseUrl ?? ""
    - TestRunner calls reporter.onRunComplete with aggregated RunResult
    - TestRunner.run() returns RunResult with correct passed/failed/cached counts
    - ProcessManager.track() adds process to tracking set
    - ProcessManager.killAll() kills all tracked processes with SIGTERM, force-kills with SIGKILL after 5s timeout
    - ProcessManager removes processes from tracking when they exit
    - setupSignalHandlers prevents double-cleanup with shuttingDown guard
  </behavior>
  <action>
    1. Write tests first (RED):

    `tests/unit/runner/test-runner.test.ts`:
       - Test: runs all tests sequentially and returns correct RunResult (2 passed, 1 failed)
       - Test: calls reporter.onTestStart with test name, reporter.onTestComplete with result details
       - Test: calls reporter.onRunComplete with aggregated data
       - Test: resolves per-test baseUrl override correctly (test-level overrides config-level)
       - Test: handles empty baseUrl gracefully (falls back to empty string)
       - Use mock Reporter and mock executeFn that returns controlled results

    `tests/unit/infra/process-manager.test.ts`:
       - Test: track() adds process, killAll() calls kill on it
       - Test: killAll() handles already-exited processes gracefully
       - Test: process removed from tracking after it exits
       - Use mock subprocess objects (with kill method, killed property, exited promise)

    2. Implement (GREEN):

    `src/runner/test-runner.ts`:
       - SuperGhost's `src/runner/test-runner.ts` implementation
       - Type `ExecuteFn = (testCase: string, baseUrl: string) => Promise<TestResult>`
       - Class `TestRunner` with constructor(config: Config, reporter: Reporter, executeFn: ExecuteFn)
       - Method `async run(): Promise<RunResult>` -- iterate config.tests sequentially. For each test: resolve baseUrl as `test.baseUrl ?? config.baseUrl ?? ""`, call `reporter.onTestStart(test.name)` (use test.name not test.case per CONTEXT.md -- name is the display label), call executeFn with test.case and baseUrl, push result, call `reporter.onTestComplete(test.name, result.status, result.source, result.durationMs)` (also use test.name for display). After loop, aggregate results and call `reporter.onRunComplete(runResult)`.
       - Helper `aggregateResults(results, totalDurationMs): RunResult`

    `src/infra/process-manager.ts`:
       - Class `ProcessManager` with private `processes: Set<Subprocess>`
       - Method `track(proc)`: Add to set, register `proc.exited.then(() => this.processes.delete(proc))`
       - Method `async killAll()`: For each tracked process, if not killed: send SIGTERM, set 5s timeout for SIGKILL, await exited, clear timeout. Use `Promise.allSettled` for all kills. Clear the set at the end.

    `src/infra/signals.ts`:
       - Export `function setupSignalHandlers(pm: ProcessManager): void`
       - Use `shuttingDown` boolean guard to prevent double-cleanup
       - Register handlers on `process.on("SIGINT")` and `process.on("SIGTERM")`
       - Handler: if shuttingDown return, set shuttingDown=true, await pm.killAll(), process.exit(130 for SIGINT, 143 for SIGTERM)
  </action>
  <verify>
    <automated>bun test tests/unit/runner/ tests/unit/infra/</automated>
  </verify>
  <done>TestRunner runs tests sequentially, calls reporter hooks in correct order, resolves per-test baseUrl overrides, returns correct aggregated RunResult. ProcessManager tracks and kills processes. Signal handlers prevent double-cleanup. All unit tests pass.</done>
</task>

</tasks>

<verification>
```bash
# All Plan 02 tests pass
bun test tests/unit/output/ tests/unit/runner/ tests/unit/infra/

# Type checking still passes with new files
bunx tsc --noEmit

# All Phase 1 tests so far pass
bun test
```
</verification>

<success_criteria>
- `bun test tests/unit/output/reporter.test.ts` passes -- ConsoleReporter box summary, duration formatting, failed test listing
- `bun test tests/unit/runner/test-runner.test.ts` passes -- sequential execution, result aggregation, reporter hook calls, baseUrl resolution
- `bun test tests/unit/infra/process-manager.test.ts` passes -- tracking, killing, cleanup
- `bunx tsc --noEmit` succeeds with no type errors
- ConsoleReporter uses picocolors for green/red/dim coloring and nanospinner for test-in-progress animation
- Box summary matches CONTEXT.md bordered layout with heavy horizontal lines
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
